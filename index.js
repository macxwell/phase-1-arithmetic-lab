
// Write your code here
// index.js

// Import necessary functions or variables from helpers.js if needed

// Define variables
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer between 1 and 100

const num3 = 14;
const num4 = 10;
const mod = num3 % num4;

const numbers = [1, 7, 13, 20, 4];
const max = Math.max(...numbers);

// Export variables or functions if required
// module.exports = { num1, num2, multiply, random, num3, num4, mod, max };
